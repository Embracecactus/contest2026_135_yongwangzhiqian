# 新硬件适配交互规则

> 以下规则适用于所有新硬件适配阶段的 AI Coding 会话。将此片段合并到项目 CLAUDE.md 中。

## 苏格拉底式澄清

- 范围、目标、硬件事实、设计选择或验收标准不清楚时，先停下询问用户。
- 不能用大量搜索代替澄清；问题回答充分后立即停止。
- 遇到架构歧义、范围扩大或破坏性操作，必须停止并上报。

## 阶段规划

每个阶段必须明确：

1. **目标（What）**：要实现/验证什么
2. **范围（Where）**：只改哪些文件
3. **验收标准（Done when）**：怎样算完成
4. **任务拆解（How）**：分几步，谁做

阶段间切换时，生成恢复提示词（含 hash/config/已验证/未验证/门禁规则），供下一阶段直接使用。

## 主模型 / Agent 分工

- **主模型**：苏格拉底澄清、规划/设计、审核综合 Agent 证据、与用户沟通决策
- **Agent**：搜索、CodeGraph 查询、文件阅读、代码修改、构建测试、hash 采集

主模型不得重复执行已委托工作。Agent 遇到架构歧义、范围扩大或破坏性操作时必须停止。

## 授权门禁

以下操作需用户明确授权：

- 构建（distclean / 完整构建）
- SDK 打包（覆盖输入文件、生成镜像）
- 刷机
- git push / 创建 PR
- 修改外层 checkout（nuttx/apps/packages/vendor）

覆盖前必须列出旧 hash。刷机后必须用板端证据验证。

## 已验证 vs 未验证

- 只有板端观察到的行为才能标记"已验证"
- 构建通过 ≠ 板端成功
- 不同构建的产物 hash 不能互相交叉引用
- "静态审查通过"只能说明代码逻辑无已知缺陷，不能替代运行时验证
