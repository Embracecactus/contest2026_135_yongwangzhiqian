# 傻妞产品化进展快照：2026-09-23

本记录用于提交当前工作、供下一轮目标评审，不是发行通过声明。
源码基点为 `166df13c4501ce466ed8178aafbc6f47deb4befe`，加本次提交保存的修改。
Agent 依赖为 `f116e08ae3f97181b8162a181d56ef5d8944f7ae`；本轮未修改该依赖。

## 当前首要阻塞

649 的 CP/AP 构建、签名、工厂打包及完整下载通过，但启动失败。
K2 RAM 驻留实验扩大了 CP 静态 RAM 占用；启动时申请 2048 字节失败，
堆统计为 total=50776、used=50728、free=48、maxfree=48，随后
`BPSR BOOT FAIL status=-12`。静态段未越界不代表运行时内存足够。
尚需核对 RAM 驻留最小闭包及启动堆预算，不能把 649 当作可用固件。
下载后没有再次自动刷写、关机或供给身份，串口已释放。

## 用户提出的问题及实际状态

| 问题 | 已取得证据 | 尚未完成 |
| --- | --- | --- |
| 回复慢、有效首句延迟 | 648 普通问答 trial-3/request=47：录音结束到首 Media 写入 16.627 秒；ASR 6175 ms、LLM 阶段 8784 ms、TTS 首 PCM 1147 ms、首 Media 写入 1629 ms（后两项从 speak 起算） | 仍等完整 LLM 正文后才 TTS；有效正文增量流水线未实现。同条件 30 轮 p50/p95、失败率与声学测量未完成，不能宣布切换模型已提速 |
| 拍照后最终回答超时 | request=48：新图 14259 字节，视觉请求 8042 ms 成功，但后续最终 LLM 请求 60016 ms 超时 -110，没有最终 TTS | TLS 收发错误诊断已补，根因未确定；拍照成功不能替代完整视觉对话成功 |
| 唤醒对快慢语速、换人泛化不足 | 训练入口增加显式 tempo augmentation，训练集正样本保持来源分组；验证/测试集不增强。已有 6 项检查通过，完成 2 epoch 训练及量化冒烟 | 内置 23640 字节模型未替换。新增 670 个变速副本来自 59 个来源组，不代表独立新说话人；公开/合法多声音数据扩充、正式训练、独立真人和板端验证待做 |
| K2 关机图标后又重启 | 648 进入 super-deep 后出现 HardFault；关机关键代码原在 XIP。649 将 PM HAL 及 WFI 路径移至 RAM，但产生上述启动 OOM | 不能宣称关机已修复。需先恢复可启动固件，再验长按三秒松手、重新开机、USB/电池差异及配置保持；功耗未测 |
| 没有重新认领二维码 | 普通 K2 关机不会撤销 owner；K2 长按五秒并未实现为恢复出厂。现存 legacy 恢复语义不等于完整用户转交入口 | 授权/物理确认、停止新工作、持久撤销旧控制凭据、多副本清理、中断恢复及新认领窗口的完整生产流程尚未收敛；不是只差现场测试 |

## 已有改善与验证边界

- App v34 / `0.7.4-shaniu-companion` 构建、单元测试通过：157 项，0 失败、0 错误、2 跳过；已保留数据覆盖安装到手机，签名与 v33 一致。
- 网络和云服务设置前置，保存按钮固定在对话框底部；保存/回读 revision 与连接状态分别显示，不用云服务未就绪冒充 BLE 断开。模型 ID 禁用输入法自动改写；减少设置页切换的隐式云配置读取。
- 复用现有认证控制会话增加只读 Wi-Fi 扫描与严格响应校验，超时/不支持提供明确提示及手动 SSID 入口。未宣称所有 BLE 生命周期回归通过。
- 配置验证兼容设备本机生成的 SEC1 P-256 私钥，同时保留实际密码库解析及证书密钥匹配；不关闭认证或证书验证。
- 禁用会中断已关联/DHCP 成功链路的 SDK 自动重连计时器，保留产品层有界重连所有权。
- 配置存储 worker 采用分离线程及停止确认，避免跨任务组 join；停止条件等待的边界仍需继续审查，不能把超时当作资源释放。
- 用户已确认 648 认领、唤醒、拍照及一次播报“完整连续”。request=47 队列 rebuffer=0、峰值 65536、最大接收间隔 782 ms、完成返回 0；这不是 30 轮稳定性验收。
- 唤醒应答的当前默认资源仍是提示音，不是人声“我在”。Media EOF 重复报错和录音结束边界错误未完成独立修复。

## 649 构建及下载证据索引

本地证据目录逻辑名为 `out/shaniu-board-20260922`，不纳入 Git。

- 实际构建命令：`python3 tools/bk7258/bk7258.py build --board aidk_ai_toy --boot mcuboot --development-identity --rollback-floor 649 --jobs 4`，退出 0；见 `build-full-649.log`。
- `release-factory-649.log`、`factory-649/release.json` 记录签名和工厂打包通过；冻结输入见 `factory-649/evidence/build-manifest.json`。
- 固件 `0.7.3+649`，安全计数 649，布局 `bk7258-510173147382a879`，复用持久开发身份 `bk7258-dev-0b4a26ff09315603c93f`，不是新信任根。
- 同板工厂 BIN 大小 8388608 字节，SHA256 `d2a648fad67bcd7ae34e37819c98f1f47aa2bd02698fc80d2928f241d16f4d62`。它含同板数据且启动失败，不作为公共发行资产上传。
- `flash-649/result.json` 五项下载成功标记均通过；Loader 原始退出 1 被封装器依据标记判为 advisory，封装器退出 0。不以此代替启动通过。
- `boot-649/serial.txt` 为启动 OOM 证据；`status-649/serial.raw` 后续无输出。端口仅为本轮本机运行参数，未写死入产品。
- `LATENCY-648.md` 与 `latency-flash-trial-3/serial.txt` 为时延证据；原始设备/语音日志保持本地，不公开上传。

## 下一轮目标应保留的验收约束

先解决 649 启动回归，再分别验证回复时延、拍照最终回答、唤醒泛化、K2 生命周期、恢复出厂/转交。
复用现有 Agent、Media、认证和工厂部署体系，不另建平行实现。
正常更新/重启/关机保留身份与配置；正式首用验收仍从受控工厂部署开始。
主机检查、编译签名打包、实际下载、正常启动、真实用户体验分别报告，不互相替代。
本次仅提交进展快照，由用户评审后确定下一轮 goal。
