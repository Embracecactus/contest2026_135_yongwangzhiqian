# 2026-07-15 15:15 RV1126B AMP 分区 NSH 复测记录

## 结论

本次复测证明：当前 openvela / NuttX HPMCU 镜像可以按 SDK 侧 `$OUT/rtt.bin` + `$OUT/amp.its` 流程重新包装为 `$FW/amp.img`，并在 RV1126B 自有板上启动到 NSH，完成 UART 控制台交互、`help` 与 `uname -a` 验证。

本记录只归档本次用户贴出的构建、AMP FIT 打包和串口验证证据。`update.img` 重新打包日志、`update.img` hash、实际 AMP 分区烧录命令和完整 `update.img` 整包刷写仍未在本记录中捕获。

## 路径约定

本文使用通用变量描述路径：

```bash
export WORKSPACE=/absolute/path/to/open-vela
export CONTEST="$WORKSPACE/contest2026_135_yongwangzhiqian"
export SDK=/absolute/path/to/rv1126b-sdk
export OUT="$SDK/output"
export FW="$SDK/output/firmware"
```

## openvela / NuttX 构建产物

本次当前 `nuttx.bin` sha256：

```text
96ed92c32fa13669589a1f067d1c9820305db3c3bcaec06d4c1744945c162cdf
```

`nuttx.bin` 大小：

```text
80320 bytes
```

该 hash 后续与 `mkimage` 输出中的 hpmcu FIT 子镜像 hash 一致。

## SDK AMP FIT 打包

本次执行的主流程：

```bash
cp -av "$WORKSPACE/nuttx/nuttx.bin" "$OUT/rtt.bin"

cd "$OUT"
"$SDK/rtos/bsp/rockchip/tools/mkimage" \
  -f amp.its \
  -E \
  -p 0xe00 \
  "$FW/amp.img"
```

`mkimage` 输出要点：

```text
FIT description: FIT source file for rockchip AMP
Created:         Wed Jul 15 15:16:10 2026
 Image 0 (hpmcu)
  Description:  hpmcu
  Created:      Wed Jul 15 15:16:10 2026
  Type:         Standalone Program
  Compression:  uncompressed
  Data Size:    80320 Bytes = 78.44 KiB = 0.08 MiB
  Architecture: ARM
  Load Address: 0x48c02000
  Entry Point:  unavailable
  Hash algo:    sha256
  Hash value:   96ed92c32fa13669589a1f067d1c9820305db3c3bcaec06d4c1744945c162cdf
 Default Configuration: 'conf'
 Configuration 0 (conf)
  Description:  Rockchip AMP images
  Kernel:       unavailable
  Loadables:    hpmcu
```

生成的 AMP 镜像：

| 产物 | 大小 | sha256 |
| --- | ---: | --- |
| `$FW/amp.img` | 83K | `04b02fda18aef333d2ed66c25b5167c713487c2de739da292d991b39902d274b` |

一致性检查：

| 项目 | 结果 |
| --- | --- |
| `$WORKSPACE/nuttx/nuttx.bin` 大小 | 80320 bytes |
| `$OUT/rtt.bin` 大小 | 80320 bytes |
| FIT `Data Size` | 80320 Bytes |
| FIT `Hash value` | `96ed92c32fa13669589a1f067d1c9820305db3c3bcaec06d4c1744945c162cdf` |
| FIT `Load Address` | `0x48c02000` |

## 板端串口验证

串口验证 transcript：

```text
nsh: ps: command not found
nsh> help
help usage:  help [-v] [<cmd>]

    .           cd          exit        mkrd        sleep       unset
    [           cp          expr        mount       source      uptime
    ?           cmp         false       mv          test        usleep
    alias       dirname     help        printf      time        watch
    unalias     dd          hexdump     pwd         true        xd
    basename    dmesg       kill        rm          truncate
    break       echo        ls          rmdir       uname
    cat         exec        mkdir       set         umount
nsh> uname -a
NuttX 0.0.0 e02f581e23 Jul 15 2026 15:15:37 risc-v rv1126b_evb
```

验证结论：

| 项目 | 状态 | 说明 |
| --- | --- | --- |
| `nuttx.bin` 生成 | 通过 | 本次大小 80320 bytes |
| `nuttx.bin` 替换为 `$OUT/rtt.bin` | 通过 | 两者大小一致，FIT hash 指向本次 `nuttx.bin` hash |
| `amp.img` FIT 包装 | 通过 | `Data Size`、`Hash value`、`Load Address` 符合预期 |
| NSH prompt / 串口交互 | 通过 | 串口可接收命令并返回输出 |
| `help` | 通过 | 命令表可输出 |
| `uname -a` | 通过 | 显示 2026-07-15 15:15:37 构建 |
| `ps` | 未启用 | 当前最小 defconfig 未启用 PROCFS；不作为 L0 基线失败项 |
| `update.img` 重新打包 | 本记录未捕获 | 用户本次未贴出 `./build.sh updateimg` 日志和 hash |
| 实际 AMP 分区烧录命令 | 本记录未捕获 | 串口结果证明板端启动成功，但精确烧录命令仍需单独归档 |
| 完整 `update.img` 整包刷写 | 未声明 | 本记录不声明完整整包刷写通过 |

## 注意事项

- 本次 `mkimage` 的 `Hash value` 已从上一份 2026-07-15 记录中的历史 hash 变为本轮构建 hash，这是预期现象；判断标准是 FIT hash 必须等于本轮 `$WORKSPACE/nuttx/nuttx.bin` / `$OUT/rtt.bin` 的 sha256。
- 本记录不改变当前 SDK patch 文档中的缺口：外部 SDK 精确 diff hunk、package-file 精确路径、实际烧录命令和完整 `update.img` 刷写验证仍待补。
