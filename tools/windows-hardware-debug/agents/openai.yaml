interface:
  display_name: "Windows Hardware Debug"
  short_description: "Capture serial logs and run guarded J-Link diagnostics"
  default_prompt: "Use $windows-hardware-debug to capture a target boot log and verify the result safely."
