# 傻妞：测试先行的分阶段实施目标

2026-09-24：用户已审阅并放行从 e3ecd6b802c010a5063f10f13cc604496ec71af6 进入业务实施。全局等待审阅暂停已解除。该放行不等于 56 项产品验收通过。

## 不可改写的证据

- 测试基线：e3ecd6b802c010a5063f10f13cc604496ec71af6。
- 原始逐例报告、四条失败、夹具与输入哈希：`tests/host/bk7258/acceptance/baseline-20260924.json` 及其引用的存档。
- Agent 固定依赖：62a304ea69c4076f0f3ef7955a9af69a5ed35277。
- 后续执行报告使用独立输出目录，保留历史文件。63 个原执行标识必须重新收集；新增、恢复复验、变异、父规格分列。

## 当前执行顺序

1. S0：先验证运行器反例，再修复 XML 与完整收集门禁；单独提交测试运行器、测试和说明。父规格分别呈现接口、L1/L2、L3 缺口。
2. S1：先复跑及补边界，再修复 K2 松手边沿和 OTA 已确认取消终态。保持认证、序号、非法帧及会话保护。四条原 Red 转 Green 后验证相关构建及两项变异。
3. 继续合同明确的退出/配置/卷切片，然后音频/Agent/显示/连接/App、N1/N2/N3、组合及最终交付。每个切片先有行为测试，不逐次等待新的 goal。

## 持续约束

- 保留原模型、前端、策略和“我在”、Canva 原生 App、独立运行能力；K2 仅关机/恢复出厂，K1/K3 仅音量。
- 不通过删断言、放宽阈值、减少收集或隐藏失败制造通过。未知接口不算有效业务 Red；只替换外部依赖。
- 未定恢复手势等参数只阻断相关功能，不能擅自定值。
- 主机/模拟器直接执行。刷写、按键、恢复出厂、掉电注入前核对实际设备/产物，说明影响与恢复并等待真实动作。当前不刷板、不清数据、不安装 APK。
- 首次工厂验收和日常保持性回归分开；不以全量重刷掩盖配置/SD 问题。
- 仅向指定 fork 的 dev-ai-contest-2026 分支提交推送，不向官方仓推送、不覆盖其他工作。
- 报告分别标明测试通过、生产实现、协议集成、实板通过、未实现及外部阻塞。主机逻辑通过不代表 HardFault、深睡或整机关机通过。


## 执行进度（新增记录，不覆盖基线）

- S0 已完成：3e04cbc8，门禁自测 12 PASS；业务未改的 63 单元复跑保留四条原始失败。
- S1 已完成：04803828，原 63 ID 全部通过，新增 5 PASS，2 变异检出/恢复；
  Android Debug 构建和另行 14 项 SDC1 协议测试通过。两次提交均已推送核对。
- S2 卷转换子切片：先复现获取中释放、释放中重复释放，再以原子转换占位修复；
  新增 3 项通过，当前集合 71 PASS，包含原 63 和共 8 个新增，不包含变异数量。
- 后续继续退出握手、配置/卷其余跨层合同。完整挂载/句柄/DMA、实板、N1/N2/N3
  仍未验收；恢复手势参数仍未冻结，只阻断相关边界。无全局暂停。
