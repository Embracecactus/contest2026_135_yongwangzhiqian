# 傻妞：测试先行的分阶段实施目标

2026-09-24：用户已审阅并放行从 e3ecd6b802c010a5063f10f13cc604496ec71af6 进入业务实施。全局等待审阅暂停已解除。该放行不等于 56 项产品验收通过。

## 不可改写的证据

- 测试基线：e3ecd6b802c010a5063f10f13cc604496ec71af6。
- 原始逐例报告、四条失败、夹具与输入哈希：`tests/host/bk7258/acceptance/baseline-20260924.json` 及其引用的存档。
- Agent 固定依赖：62a304ea69c4076f0f3ef7955a9af69a5ed35277。
- 后续执行报告使用独立输出目录，保留历史文件。63 个原执行标识必须重新收集；新增、恢复复验、变异、父规格分列。

## 当前执行顺序

1. S0：先验证运行器反例，再修复 XML 与完整收集门禁；单独提交测试运行器、测试和说明。父规格分别呈现接口、L1/L2、L3 缺口。
2. S1：先复跑及补边界，再修复 K2 松手边沿和 OTA 已确认取消终态。保持认证、序号、非法帧及会话保护。四条原 Red 转 Green 后验证相关构建及两项变异。
3. 继续合同明确的退出/配置/卷切片，然后音频/Agent/显示/连接/App、N1/N2/N3、组合及最终交付。每个切片先有行为测试，不逐次等待新的 goal。

## 持续约束

- 保留原模型、前端、策略和“我在”、Canva 原生 App、独立运行能力；K2 仅关机/恢复出厂，K1/K3 仅音量。
- 不通过删断言、放宽阈值、减少收集或隐藏失败制造通过。未知接口不算有效业务 Red；只替换外部依赖。
- 未定恢复手势等参数只阻断相关功能，不能擅自定值。
- 主机/模拟器直接执行。刷写、按键、恢复出厂、掉电注入前核对实际设备/产物，说明影响与恢复并等待真实动作。当前不刷板、不清数据、不安装 APK。
- 首次工厂验收和日常保持性回归分开；不以全量重刷掩盖配置/SD 问题。
- 仅向指定 fork 的 dev-ai-contest-2026 分支提交推送，不向官方仓推送、不覆盖其他工作。
- 报告分别标明测试通过、生产实现、协议集成、实板通过、未实现及外部阻塞。主机逻辑通过不代表 HardFault、深睡或整机关机通过。


## 执行进度（新增记录，不覆盖基线）

- S0 已完成：3e04cbc8，门禁自测 12 PASS；业务未改的 63 单元复跑保留四条原始失败。
- S1 已完成：04803828，原 63 ID 全部通过，新增 5 PASS，2 变异检出/恢复；
  Android Debug 构建和另行 14 项 SDC1 协议测试通过。两次提交均已推送核对。
- S2 卷转换子切片：先复现获取中释放、释放中重复释放，再以原子转换占位修复；
  新增 3 项通过，当前集合 71 PASS，包含原 63 和共 8 个新增，不包含变异数量。
- 后续继续退出握手、配置/卷其余跨层合同。完整挂载/句柄/DMA、实板、N1/N2/N3
  仍未验收；恢复手势参数仍未冻结，只阻断相关边界。无全局暂停。
- S3：真实 Agent 采集退出新增 2 项基线通过，总集合 73 PASS；额外 2 个隔离
  错误变异检出及 2 次恢复复验单列。生产与依赖未改。下一切片是产品电源
  协调器的部分退出/恢复失败生产路径测试，而非继续把局部通过当整机证明。
- S4：产品电源协调器原样拆出测试接缝，再修复三个停止边界失败；总集合
  78 PASS。AIDK AP 产品对象编译通过，未链接/签名/部署固件。后续重点是
  正式失败状态快照与实际资源退出整合；故障提示及失败时 KWS 清理仍有缺口。
- S5：纠正入口/存储失败遗漏 KWS 停止及用 started 推测退出完成的问题；新增
  四项中三条先 Red，再完整复跑 82 PASS。AP 产品对象编译通过。真实 KWS/Media
  联合退出、阻塞时间及正式失败状态展示仍未完成，不关闭相关父需求。
- S6：核对真实 quiesce 返回语义后，修正 S4/S5 把暂忙当永久失败的缺口；保持
  已受理配置/网络清理可推进。两条新反例转绿，总集合 84 PASS，AP 对象通过。
  全链路持久提交及真实设备退出仍未验证；历史失败与前序报告保留。
- S7：SDC1 单向停止写入门禁已实现，5 项真实 parser 测试通过，总集合 89 PASS；
  非法帧/认证/序号及暂存取消保持。接口缺失前置编译未算业务 Red。下一步必须
  接入生产 owner 的分阶段退出；目前只是协议能力，不是设备关机查询完成。
- S8：S7 门禁已接入真实 owner/电源路径，保留准备阶段已认证查询，资源退出后
  最终关闭传输再请求 CP。真实协调器+owner+SDC1 主机联动通过，总集合 95 PASS；
  既有真实 TLS 回归通过，AP product/owner 对象通过。实板 BLE/睡眠及恢复仍未验。
- S9：关机失败显示意图及内置 X 图标已补，原协调器反例转绿；冻结源码重跑
  97 PASS，AP product/display 对象通过。实际屏幕确认仍待验。只读设备枚举当前
  无串口、ADB 仅模拟器，未发现历史 Mi 10；不扩大成主机工作的全局阻塞。
- S10：补真实配置/存储/HTTP 的文件同步失败、显式重试与目录同步未知测试；
  修正 receipt 观察器错误并保留原失败，校正后 100 PASS。生产未改，未知结果
  的安全恢复仍待实现。Mi10 已重新在线（App42/0.7.12），COM9可见待核对板身份。
- S11：遵守未知发布必须跨启动恢复的既有契约，新增两个真实存储新进程
  加载用例，分别保留旧/新有效版本与凭据。102 PASS，生产未改；目标 LittleFS
  掉电、实际重启和用户可见恢复未验证。继续卷/句柄交接及后续业务切片。
- S12：修复 MSC 后端 USB 退出失败仍释放 inode、无法再次退出的问题；
  真实 stop 函数反例先红后绿，104 PASS，AP MSC 对象通过。真实端点/DMA、
  初始化失败清理、统一外部卷文件系统及句柄交接仍未验，不关闭MSC父需求。
- S13：补 MSC 启动失败资源保留及模式回滚隔离，两个真实失败修复；保留
  CDC 初始化重试的旧契约。106 PASS，AP MSC/mode对象通过；真实DMA、块设备
  close失败和统一卷管理仍未验，不将该片等同MSC完整验收。
- S14：真实Media适配器增加尾部PCM及取消后新会话样本账本，108 PASS；
  丢尾部变异检出/恢复单列，生产未改。仅验证驱动提交字节，不等于真实播放、
  完整prepare或声学验收。继续音频/Agent与共同服务实施。
- S15：N2专注计时生产核心及SDC1 kind10/产品循环已接线，112 PASS；
  AP product/control/focus对象通过。App、显示/完成提示、NFC及跨重启策略
  仍待完成；新接口首次缺失不算业务Red，计时状态不等于实际提醒交付。
- S16：原生App专注入口和共享会话控制器已接线，ACK后回读、不一致快照拒绝、
  断线不重放及事务隔离用例通过；117 PASS，Debug APK构建通过未安装。
  UI视觉/真实BLE、设备提示及NFC仍待验，跨重启策略待用户确认。
- S17：专注环/眼睛、暂停与完成视觉提示已接现有显示worker，119 PASS；
  AP product/display/focus对象通过，主机像素预览已检查。真实双屏/DMA/调度、
  资源预算和NFC仍待验；完成提示仅视觉，跨重启策略仍待确认。
- S18：模拟器真实View复现并修复专注分钟草稿在关闭/重开时丢失；20轮导航
  和一次Activity重建通过，相关5个JVM用例通过，APK构建通过。仅恢复分钟草稿，
  不恢复设备操作；Mi10未安装，真实BLE/大字体键盘及NFC仍未验。119项旧报告未改写。
- S19：正式构建入口完成CP/AP/BL1/BL2增量构建与清单重新校验，ELF含计时及
  显示入口；冻结119单元全绿，两项既有变异检出/恢复。保留原开发信任和661
  安全计数；依赖覆盖修改已记录。未签名打包/刷板，当前板信任与冷构建仍未验。
- S20：核对NuttX close_blockdriver的引用消费契约；补真实MSC后端关闭错误、
  再清理及重新打开回归，120 PASS。保留已消费inode的隔离变异被检出，恢复通过；
  生产不变。统一卷挂载/文件系统、介质持久性和实板DMA仍未实现或待验。
- S21：显示状态查询脱离挂载/解码/绘屏锁，改读最近完成更新的短锁快照；
  持有渲染锁的真实getter反例转绿，121 PASS，CP/AP完整链接及清单复验通过。
  新增静态112字节，无新线程/堆分配。其他查询、异步安装和实板调度仍待完成。
- S22：语音eyes工具改投递单槽表情意图，现有显示worker执行；受理/完成分开，
  最近请求结果可查，电源/认领及后来的明确设置取消未执行请求。122 PASS，
  错把受理当完成的变异检出，完整链接通过。视觉同步路径、期限、整轮延迟和
  实板仍待完成；新接口初次缺失记BLOCKED_INTERFACE，不冒充旧业务Red。
- S23：表情取消按精确ID处理；显式控制取消捕获旧ID，工具投递前后复查取消，
  running不谎报canceled。123 PASS，旧取消误伤新请求/漏后检查两变异检出，
  完整链接和清单复验通过。无回合ID的完成事件未用于取消，期限及实板仍待验。
- S24：准备场景存储时发现云模型保存把EINPROGRESS读回洗成成功；真实偏好/
  SCF1/POSIX目录同步故障反例转绿。未知状态同进程保持，重启及正式重置完成
  恢复边界已补。124 PASS，完整链接/清单复验通过；实板掉电、App提示及NFC仍未验。

- S25（2026-09-25）：原生配置编辑器在提交中/结果未知/本地回执未确认时禁止再次保存，
  保留回读；模拟器真实Views失败转绿。124个既有执行单元通过，新增1个UI用例单列，
  Android单元测试/APK构建通过。Mi 10在线但未安装，BLE/实板持久化仍待验证。

- S26：本地动作采样入口未就绪时清除上一成功样本，统一错误响应；真实服务入口
  失败转绿，既有124单元保持通过，传感器RPC另列20场景（新增1）。增量构建、
  层次与构建清单校验通过。N1自主识别/短时恢复及实板动作数据仍未完成。

- S27：真实视觉反馈/显示命令的同名新选择反例转绿；以非复用身份保护结果更新和
  延迟恢复，取代名称比较。125 PASS，移除身份校验的隔离变异检出，增量构建/
  清单通过。试用TTL待确认；该片不是完整试用、场景优先级或实板渲染验收。

- S28（2026-09-27）：参数化限时表情试用接入显示worker，期限含排队时间，取消/
  恢复/新身份优先及失败终态有生产模块测试。126 PASS，两项变异检出，构建/清单
  通过。新增接口起初BLOCKED_INTERFACE；尚无协议/App调用，不能称用户可用试用。
  下一片接已认证配置入口，再接原生App；默认TTL未代用户确定。

- S29：ETC1/ETS1试用协议接真实认证Session与产品config入口，锁内检查旧ID、
  重复操作不延长期限、取消受理与恢复完成分开；127 PASS，两变异检出，AP实际
  保留调用符号。重连夹具漏close的失败另存并纠正。原生App入口及实板仍待完成。

- S30（2026-09-27）：原生App新增限时表情试用，真实Session提交ETC1并分段核对
  ETS1；不将受理当已显示/已恢复。用户明确输入时长，关闭页面不取消设备试用，
  草稿可重建且不重发操作。8个新增JVM用例及模拟器20轮导航/重建通过；严格集合
  135 PASS，原63标识保留，两项既有变异继续检出。NuttX不支持错误提示Red转Green。
  APK构建通过；当前ADB仅模拟器，手机/板端BLE与真实渲染仍未验证。下一切片继续
  场景共同入口及NFC低风险绑定的测试与接线，未定参数仅阻断对应行为。

- S31：场景卡前置检查发现NFC将零字节/异常长度和HCE正值误报present；三个
  生产核心反例Red转Green，保留并纠正旧“零字节成功”断言依据。138 PASS，原63
  保留；RPC原19场景通过，目标增量构建/清单通过。两次运行器标记接线错误原样
  归档。卡片绑定/驻留去重/共同场景入口仍未实现，驱动选卡及实板兼容性仍待验。

- S32：NFC扫描改用标准GET_PICC_UID，传播选卡错误并拒绝非法UID长度/未完成位；
  UID留在worker局部，RPC仍只返回presence。真实client/service/core的8个新增
  场景Red转Green，严格146 PASS、原63保留；完整NFC RPC 27场景与motion RPC
  20场景通过，目标增量构建/清单通过。实际配置未启用ISO-DEP/frame能力，不将
  编译中的HCE分支称为已可用。绑定/驻留去重/场景派发与实板卡型仍需推进。

- S33：专注计时的FOC1/FOS1改为同一typed execute/snapshot服务的薄适配，保留
  原版本/幂等/取消规则。四组跨入口生产路径测试通过，严格150 PASS、原63
  保留；断开wire实际应用的变异被检出，构建/清单与AP符号核验通过。单一状态
  仍为88字节；卡片/语音调用与产品owner投递尚待接线，未称N2或实板完成。

- S34：新增focus_timer语音工具，经单槽待办进入原产品owner，复用同一专注服务；
  工具受理不冒充开始/完成，查询只读带时间的缓存。电源/重置准备撤销未执行
  操作，过期revision失败，执行后取消明确too_late。155 PASS，原63保留；两项
  新变异检出，构建/清单/AP入口核验通过。新增静态92字节，无新线程；实际Agent
  回合/ASR/声学/实板未验，卡片绑定与低风险场景入口继续待接。

- S35：关闭S34新增focus_intent源/头文件的格式缺口；固定nxstyle源码以本仓
  TOPDIR构建并完整检查两文件通过，未关闭规则。实际重编译后的目标对象逐字节
  等同S34，155项原集合全绿，构建/清单通过。无业务语义或验收范围变更；历史
  其他文件格式未在此片重查，NFC绑定及实板等原缺口保持。
