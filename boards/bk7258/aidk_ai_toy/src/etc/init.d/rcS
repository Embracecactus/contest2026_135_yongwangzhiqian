/****************************************************************************
 * boards/bk7258/aidk_ai_toy/src/etc/init.d/rcS
 *
 * SPDX-License-Identifier: Apache-2.0
 ****************************************************************************/

#include <nuttx/config.h>

#if defined(CONFIG_BK7258_PRODUCT_KEYS) && !defined(CONFIG_BK7258_AP_CORE)
/* 按键服务独立于网络与语音，旧 voice/PTT 命令不再进入产品启动链。 */
bkkeys
#endif

#ifdef CONFIG_DOLPHIN
/* List compiled local tools; startup does not transmit or scan RF. */
dolphin list
#endif

/* CP services added here must check their own runtime prerequisites: NSH
 * calls this script after final-init even if its diagnostic check failed.
 */

#ifndef CONFIG_NSH_DISABLE_ECHO
echo "BK7258 RCS PASS"
#endif
