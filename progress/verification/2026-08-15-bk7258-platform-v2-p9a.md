# P9a shadow/parity verification

Date: 2026-08-15

Scope: the host-only BK7258 legacy-profile shadow ledger, framework smoke
command, and release/evidence checklist.  This record does not authorize P9b
cutover or any device operation.

## Implemented contract

- `board/bk7258/scripts/bk7258_shadow_ledger.json` is a strict
  `bk7258.shadow/1` 27-row mapping bound to the frozen P0 manifest and
  migration-ledger digests.  Allowed row statuses are exactly `EXACT`,
  `EQUIVALENT_WITH_REASON`, `MIGRATION_PENDING`, and `RETIRE_PROPOSED`.
- `shadow-check` iterates every frozen profile and reports old metadata,
  resolved defconfig, source closure, device nodes, resource claims, SDK role,
  and package-plan evidence.  Unavailable fields remain explicit nulls; no
  row is reported as equivalent or exact without evidence.  The current 27
  rows are all `MIGRATION_PENDING` with rationale.
- `framework-check` covers P0-P8 metadata/schema invariants and generates
  representative T5 and AIDK plans/package metadata plus a dry transport plan.
  It is bounded and does not invoke a production build or test campaign.
- The release checklist records fork/PR separation and the later, scrubbed
  local-staging-to-`logs/` export gate.  No logs were exported or created.

## Exact checks

Commands run from the repository root:

```text
python3 -m unittest -v board/bk7258/tests/test_bk7258_framework.py
python3 board/bk7258/scripts/bk7258_framework.py --root . shadow-check --out /tmp/bk7258-shadow-git.json
python3 board/bk7258/scripts/bk7258_framework.py --root . framework-check --out /tmp/bk7258-framework-check.json
python3 -m py_compile board/bk7258/scripts/bk7258_framework.py
git diff --check
```

Expected/observed status:

```text
FRAMEWORK_TESTS_PASS (13 tests)
SHADOW_CHECK_PASS (27 rows; MIGRATION_PENDING=27)
FRAMEWORK_CHECK_PASS (11 bounded checks)
PY_COMPILE_PASS
DIFF_CHECK_PASS
BUILD_NOT_RUN
SIGNING_NOT_RUN
FLASH_NOT_RUN
HARDWARE_NOT_ACCESSED
NETWORK_NOT_USED
LOG_EXPORT_NOT_RUN
```

## Boundaries

- The frozen `board/bk7258/configs` tree and existing SDK bytes remain
  unchanged.  No profile, product directory, generated build seed, package,
  signing key, or trust material was added.
- P9a shadow success is not P9b authorization.  Device nodes and resource
  claims are only available from proposed metadata where present; missing old
  evidence is not treated as an empty claim set.
- No push, PR, public NuttX change, private mirror mutation, build, signing,
  Flash, serial open, or hardware boot was performed.
