# BK7258 T5-Board TF/SDIO host verification

Date: 2026-08-14

## Scope

Adapt the T5-Board V1.0.2 TF socket as two explicit hardware profiles:

- one-bit SDIO while retaining the CH342F UART0 download route;
- four-bit SDIO only after S1-1/S1-2 isolate that UART0 route.

This record covers schematic/protocol review, board/chip separation, profile
preflight, signed builds, non-halting level sampling and apps-only hardware
validation.  COM4 is not opened and the installed boot chain is not written.

## Hardware contract

- TF CLK/CMD/D0/D1/D2/D3 are P2/P3/P4/P5/P10/P11.
- The schematic labels P6 as active-low card detect, but the physical board
  reads P6 high both with media inserted and removed.  P6 therefore remains
  schematic metadata and is not used as a presence source.
- S1-1/S1-2 select the conflict on P10/P11 between TF D2/D3 and the CH342F
  UART0 download route.  They do not control the P0/P1 SWD/RTT route.
- Optional serial Flash U3 is marked NC.  If fitted, it shares the TF bus and
  the TF socket is not available to this profile.

The optional Beken SDK insertion helper enables an input pull-up and returns
the raw GPIO level; its caller treats high as no card.  SDK builds without
`CONFIG_SDCARD_CHECK_INSERTION_EN` probe without this check.  The same SDK
documents that a card powers up in one-bit mode and sends ACMD6 before moving
to four-bit mode:

- `ap/middleware/driver/sd_card/sd_card_driver.c:112`
- `ap/middleware/driver/sd_card/sd_card_driver.c:462`

## NuttX protocol contract

The NuttX MMC/SD upper half sends ACMD6 only when
`SDIO_CAPS_1BIT_ONLY` is clear, then invokes `SDIO_WIDEBUS(..., true)`.
On media removal it unregisters the block device and restores one-bit mode:

- `nuttx/include/nuttx/sdio.h:478`
- `nuttx/drivers/mmcsd/mmcsd_sdio.c:2732`
- `nuttx/drivers/mmcsd/mmcsd_sdio.c:2781`
- `nuttx/drivers/mmcsd/mmcsd_sdio.c:4389`

Therefore a one-bit lower half must return `SDIO_CAPS_1BIT_ONLY`; returning
zero would incorrectly allow the upper half to command the card into four-bit
mode while the controller remained one-bit.

For a slot without insertion information, NuttX requires `SDIO_PRESENT()` to
remain true and probes once during slot initialization.  Consequently the
T5-Board profile disables `MMCSD_HAVE_CARDDETECT`, does not claim hotplug and
requires the card to be inserted before reset.

## Implementation

- The validated lower half reports `SDIO_CAPS_1BIT_ONLY`.  The reserved
  four-bit path would report `SDIO_CAPS_4BIT`, initialize in one-bit mode and
  leave ACMD6 plus `widebus` to NuttX, but it is rejected with the current SDK
  bundle as described below.
- The hidden chip host owns only the controller protocol.  T5-Board owns the
  slot binding, pin mux, fixed-media policy and S1 route assertions.
- T5-Board status is always present after board initialization.  The callback
  ABI remains implemented, but no P6 poll worker is compiled or queued.
- The bounded validator first mounts `/dev/mmcsd0` as FAT.  If the medium has a
  GPT protective MBR, it registers up to 32 partitions and tries them from
  largest to smallest until the FAT driver accepts one.  It then creates one
  unique 4096-byte file, calls `fsync`, closes and reopens it, verifies the
  pattern, removes only that file, unmounts, and repeats once on the same fixed
  media.  It never formats the card.
- The controller retains continuous card clocking across CMD7 and the normal
  transfer-clock switch, matching official v3.1.1.9 and Tuya behavior.  CMD12
  alone retains the SDK's reset/gate/reset sequence.  Command polling clears
  only command W1C status and preserves a coalesced data IRQ.
- T5 TF profiles use `MMCSD_MULTIBLOCK_LIMIT=1`.  NuttX therefore issues
  CMD17/CMD24 for media I/O and does not expose the immutable CPU-FIFO SDK's
  observed second-block pre-read overflow to the filesystem.
- Four-bit selection requires
  `CONFIG_BK7258_T5_BOARD_SDIO_D2_D3_ISOLATED=y`; paired-profile preflight also
  rejects an active CP/BL1 UART0 conflict.

## Host verification

The artifact details immediately below are the historical pre-level-sampling
baseline.  Their P6 polling policy was superseded by the fixed-media correction
recorded later.  The four-bit artifact also predates discovery that the SDK
data-setup helper silently forces one-bit operation; it proves only the
NuttX-facing build semantics and is not a runnable four-bit result.

Both profile pairs passed the build script's profile/route preflight:

- `t5_board_cp_app_mcuboot + t5_board_ap_tf_1bit_validation_mcuboot`
- `t5_board_cp_app_mcuboot + t5_board_ap_tf_4bit_validation_mcuboot`

Both AP profiles then completed a clean configure, compile, and link while
holding `/tmp/openvela-bk7258-build-1000.lock`.

### One-bit artifact

- AP raw: 151372 bytes,
  SHA-256 `1948abb22f8202af60ce3eae5226c983cdb5ac374ea70bcd81d4847e9e01c1ee`
- AP CRC: 160854 bytes,
  SHA-256 `88886ff0a185c335958e2de28dd92db231424093717e5e3d1fdca2d6fab2dca1`
- ELF: 938044 bytes,
  SHA-256 `93256df064389dfc4c6d6c64fd6e76501c25c47ef7f8ab3616176787fa101615`

Resolved configuration contains SDIO, 100 ms media polling, card detect,
HPWORK, FAT, block setup, and the TF validator; it does not contain either
four-bit assertion.  ELF disassembly shows
`bk7258_sdio_capabilities()` returning `0x01` and P6 input being inverted for
active-low presence.

### Four-bit artifact

- AP raw: 151380 bytes,
  SHA-256 `3e51f32ff46cf53358898c0ffa7616c6e0c9e8f548bd977fad586ae5b4c1bd50`
- AP CRC: 160854 bytes,
  SHA-256 `d137a3316265113ceb4427aefd39c2ddd15879bbaefd2d57e56e765cd62415ba`
- ELF: 938044 bytes,
  SHA-256 `db724fd909b6444b788c6ffa3ea30544044cebcdde4dc43883a14bcc9f1fd140`

Resolved configuration contains both `CONFIG_BK7258_SDIO_4BIT=y` and the
D2/D3 isolation assertion, plus the same card-detect/HPWORK/FAT contract.
ELF disassembly shows:

- `bk7258_sdio_capabilities()` returns `0x08` (`SDIO_CAPS_4BIT`);
- `bk7258_sdio_initialize()` passes zero/false to the initial host setup;
- `bk7258_board_sdio_card_present()` reads P6 and inverts the level;
- validator state, validator initialization, and media worker symbols are
  retained in the final image.

The dual-image script was also made fail closed at argument parsing:
`--help` exits successfully without touching build state, while an unknown
argument exits 2 before configuration or build.

The host-only gates at that checkpoint passed:

- shell syntax check;
- both then-permitted paired-profile checks;
- project-memory structure check;
- `git diff --check`.

NuttX checkpatch on the new validator found only the repository's known
custom-tree path-format diagnostic.  The previously existing SDIO source and
board wrapper still carry their baseline path/layout/long-line diagnostics;
this stage does not claim a clean upstream-style gate.

### Signed one-bit package

The paired `t5_board_cp_app_mcuboot +
t5_board_ap_tf_1bit_validation_mcuboot` build completed through the existing
signed MCUboot pipeline with version `18.6.48` and security counter `102`.
Factory and RPTUN layout checks passed.  The staged package records:

- CP raw SHA-256:
  `7e2a0e1ceb1aa54514655a5293b28fb04db5472970f17da10c0934344aac3209`
- AP raw SHA-256:
  `db46abbf99b9198e327c9ea2b3d73db6e667d282c1dedb5be8c043113e4ad497`
- CP Flash segment SHA-256:
  `5cf564fda845e84ef685ce520ab688b3d9ad26c9b3592df902c1c2e09591d3f1`
- AP Flash segment SHA-256:
  `522e681b421433e1cc017438acde4ecf0dff2adb5589b6995736b9feaa5c012e`
- Public trust-contract SHA-256:
  `b2a8e35256b97a0dd4fef6da253225d770001f4f20743d44bf8e9cc5f3b97c7e`

No image from this TF stage had been downloaded or run on the board at this
checkpoint.

## One-bit real-board download and first boot

The existing download entry ran the merged non-halting J-Link trust preflight
before `bk_loader`; all three installed BL1/BL2 public fingerprints matched.
The apps-only write set contained exactly:

- CP `app_crc_flash.bin@0x11000-0x3d000`
- AP `app1_crc_flash.bin@0x165000-0x28000`

Both erase/write operations passed.  BL1, BL2, manifests, secondary slot,
LittleFS and other data regions were not included in the loader arguments.
COM3 was used for download and COM4 was not opened.

After reboot the selected MCUboot pair reached the intentional BL2 SWD hold at
PC `0x280205a2`.  Writing the existing SRAM release token allowed CP/AP to
start; CP RTT channel 0 produced an NSH prompt and the AP validation diagnostic
became valid with bus width one.

The card was physically inserted throughout this boot, but P6 read high and
the diagnostic ended as state `FAILED`, stage `WAIT_INITIAL`, result
`-ETIMEDOUT`, cycles/bytes zero.  Board initialization completed and the SDIO
controller was active, so this exposes an unresolved mismatch between the
assumed active-low card-detect contract and the actual T5-Board CD path.  The
worker never reached mount or media I/O; this run establishes correct
download/boot/worker startup but neither passes nor fails SDIO/FAT operation.
A card-removed P6 sample was still required at this point; the next section
records that result and supersedes the unresolved CD conclusion.

## Removed-card level and fixed-media correction

With the card removed, four consecutive non-halting J-Link reads of the AON
GPIO6 register at `0x44000418` returned `0x00010009`; input bit 0 remained high.
To eliminate firmware pin-configuration differences, the register was
temporarily set to the SDK-equivalent input plus pull-up state.  Four reads then
returned `0x0015003d`, again with input bit 0 high, after which the original
`0x00010009` value was restored.  No flash region was written.

Combined with the earlier inserted-card high level, this proves that P6 has no
usable insertion transition on the tested T5-Board.  Reversing active-low to
active-high would incorrectly report both physical states as present and was
therefore rejected.

The correction separates the layers as follows:

- hidden `BK7258_SDIO` remains the chip host/controller capability;
- `BK7258_T5_BOARD_TF_SLOT` owns the physical slot and selects the host;
- board config declares card detect unavailable and leaves P6 untouched;
- T5-Board profiles explicitly set `MMCSD_HAVE_CARDDETECT=n`;
- the validator runs two same-card FAT cycles without claiming hotplug;
- profile preflight rejects a T5-Board TF profile that omits the fixed-media
  upper-half policy.

The corrected one-bit CP/AP pair then completed a full signed MCUboot build
with version `18.6.49` and security counter `102`.  SDK checksum, partition,
Factory and RPTUN layout gates passed.  The public trust-contract SHA-256
remained `b2a8e35256b97a0dd4fef6da253225d770001f4f20743d44bf8e9cc5f3b97c7e`.
The resolved AP configuration contains the board TF slot, SDIO, HPWORK and FAT,
but explicitly omits `MMCSD_HAVE_CARDDETECT`; the final ELF retains the fixed
card-present hook and validator diagnostic while omitting the media-poll worker.

## Development signing identity recovery

The original temporary working filenames disappeared, but the keys had been
created by the earlier project workflow rather than supplied by an external
owner store.  Existing current-domain alias copies were checked against the
three recorded public fingerprints and matched exactly:

- BL2 MCUboot SPKI SHA-256:
  `df8b7893df6afea30da3b212f26f42646404e5e7d9feae0ec2615d0c90cea26e`
- BL1 SEC1 SHA-256:
  `6d17866e4d3572eff3a9b9fc1c8d33b9a1add71a13784b2feff385c38883106c`
- BL1 X||Y SHA-256:
  `e82597007f132d2415c64ea88f0dc75bce67e574d6e9943931a0b97a849fcc02`

The working filenames were restored from those same-key copies.  No new key,
unrelated trust domain or root rotation was used.  The root cause was keeping
agent-generated development keys under temporary names and then treating one
missing name as proof that the identity was gone.  The merged public-contract
and non-halting J-Link preflight, not a filename, remain authoritative.

## SDIO failure localization and correction

The first fixed-media boots reached `/dev/mmcsd0` but timed out before a FAT
read.  Non-halting register snapshots and command-specific error logs separated
three independent issues:

1. The wrapper had started clearing `clk_gate_on` when switching from ID to
   transfer clock.  The last completed response remained CMD7 and register
   `0x458d0044` confirmed automatic clock gating.  Official v3.1.1.9, Tuya and
   the historical wrapper all keep continuous clocking through CMD16 and SCR.
   Removing the ordinary transfer-clock gate change allowed enumeration and
   sector reads to proceed.
2. Multi-block CMD18 then overflowed while the immutable SDK's CPU-FIFO path
   pre-read the second block.  Waiting before draining and reducing the clock
   to 13 MHz did not close the block-boundary race.  The final profiles use the
   standard NuttX `MMCSD_MULTIBLOCK_LIMIT=1` policy at the board/profile layer,
   retain 20 MHz, and use single-block CMD17/CMD24 transfers.
3. The test card is a Home Assistant OS GPT medium.  The first GPT fallback
   incorrectly assumed its largest partition was FAT and received `-EINVAL`.
   The validator now tries parsed partitions until the FAT driver accepts one;
   it selected `/dev/mmcsd0p1`, named `hassos-boot`.

`/dev/mmcsd0` existing by itself was not treated as proof of completed SD
initialization: the NuttX probe can register the block driver after CSD parsing
even when a later SCR/transfer step fails.

## Final one-bit build and hardware result

The final signed build used:

- CP profile: `t5_board_cp_app_mcuboot`
- AP profile: `t5_board_ap_tf_1bit_validation_mcuboot`
- MCUboot version: `18.6.60`
- security counter: `114`

SDK checksum, partition, Factory-layout and RPTUN-layout gates passed.  The
resolved AP configuration contains `CONFIG_MMCSD_MULTIBLOCK_LIMIT=1`.  The
public trust-contract SHA-256 remained
`b2a8e35256b97a0dd4fef6da253225d770001f4f20743d44bf8e9cc5f3b97c7e`.

The normal non-halting trust preflight passed before an apps-only download.
The loader write set was exactly:

- CP at `0x11000`, length `0x3d000`
- AP at `0x165000`, length `0x28000`

The exact downloaded sparse segments were recorded before the loader ran:

- CP SHA-256:
  `42e7089d8bf39dc269e09515a46cfcf34fa502774c5349cdd84d17fffc206922`
- AP SHA-256:
  `5db0488d5c5aa5483cab062490d1eafc527f1b85b1cac473101c17331256875c`

BL1, BL2, manifests, secondary slot, LittleFS and other data regions were not
written.  The download log is
`/home/lijian/project/open-vela/logs/bk7258-auto-debug/20260814-214334`.

The final non-halting J-Link read at diagnostic address `0x28054b08` returned:

```text
46544B42 00240001 00000002 00000000
0000000A 00000001 00000002 00002000
17C60DC5
```

Decoded result:

- magic `BKTF`, version 1
- state 2 (`PASS`), result 0
- final stage 10 (`UNMOUNT`)
- bus width 1
- two completed cycles
- 8192 validated bytes
- checksum `0x17c60dc5`

RTT captured the GPT candidates and the final FAT selection in
`C:\Users\lijian\AppData\Local\Temp\bk7258-tf-1bit-18.6.60-pass.log`.
The 1024-byte RTT ring truncated the textual PASS tail, but the independent
diagnostic block above records both completed cycles and final unmount.  The
temporary test file was removed in each cycle.  COM3 supplied reset/download;
COM4 was not opened, and P0/P1 SWD/RTT remained available.

## Four-bit boundary

Four-bit is not claimed as validated.  The current AP `libdriver.a` bundle
defines SDIO V2 but not `CONFIG_SDCARD_BUSWIDTH_4LINE`; its private data-setup
helpers overwrite the runtime width from that compile-time macro and therefore
force one-bit operation.  A genuine four-bit stage needs a separately identified
four-bit-capable SDK bundle or an SDK-side ABI correction, followed by S1-1/S1-2
UART0 isolation and a fresh signed build plus real-board validation.  Merely
selecting the NuttX four-bit profile is insufficient.  Both the build preflight
and the wrapper now fail this combination closed rather than generating a
misleading four-bit image.

## Final source-tree gates

After adding that fail-closed guard, the same one-bit pair was rebuilt through
the complete signed pipeline again with version `18.6.60` and security counter
`114`.  SDK checksums, CP/AP compile and link, MCUboot signing, partition,
Factory-layout and RPTUN-layout checks all passed.  This post-guard rebuild was
not downloaded because the guard is inactive for one-bit and the preceding
18.6.60/114 board run had already completed the hardware result above.

The post-guard build generated CP/AP sparse-segment SHA-256 values
`783b6a67b1052e8b1bb21daf2cabe761e8568d0429a95e74eb219e689e41f67e`
and `7422a2db69998184dae38604edfb7431fa85fb2cadcc5e881088a9fa80c8dfbe`.
They are recorded separately from the downloaded hashes above; no claim is
made that two development rebuilds with embedded build metadata are
bit-for-bit identical.

The final focused checks are:

- one-bit profile-only preflight: PASS;
- four-bit profile-only preflight: expected exit 2 with
  `pinned AP SDK bundle forces one-bit data setup`;
- resolved AP config: `MMCSD_HAVE_CARDDETECT=n`, board TF slot enabled,
  `MMCSD_MULTIBLOCK_LIMIT=1`, GPT partition support enabled;
- `bash -n board/bk7258/scripts/build_dual_image.sh`: PASS;
- `git diff --check`: PASS.
