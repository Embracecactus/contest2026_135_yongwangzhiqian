# N15 Tier-2 paired OTA task

- Status: in-progress
- Started: 2026-08-03
- Branch: `feat/bk7258-n15-ota`
- Base: `origin/dev-ai-contest-2026` at `8738f078dfd7e571f646b776352123f4029721e0`
- Current gate: ADR-006 format-2 dual-bank A-to-B-to-A rotation and the ordered
  42..57 campaign are host/source/ELF/dry-run-verified; physical fault
  injection, both lifecycle directions and final board restoration require
  fresh owner authority

## Completed

- Source/binary/Ghidra verification of exact official v3.1.1.9 RBL, AB
  partitions, one-offset remap and one-trial semantics.
- Rejected ADR-003 paired physical-sector swap before it reached hardware;
  archived its read-only R2 evidence.
- Accepted ADR-004 official-style contiguous CP/AP A/B layout.
- Moved AP XIP to `0x02150000`, moved raw LittleFS to
  `0x600000..0x700000`, and updated all team-owned link/boot/MTD/pack/debug
  contracts without modifying official sources or SDK libraries.
- Added canonical layout and independent byte-exact factory verifiers.
- Built exact v3.1.1.9 `cp_nsh_psram + ap_smp_psram`; all SDK, RPTUN, BLE
  and PSRAM host gates passed.
- Corrected the migration from an unsafe dense `0x700000` file to two
  bounded segments so `usr_config`, reserved ranges and calibration tail are
  absent from the loader write set.
- Performed the owner-authorized board migration and N15-M regression:
  NSH, AP SMP, RPTUN, LittleFS, PSRAM, timer, RPMsg, RPMsgFS, Bluetooth and
  3/3 physical resets passed.
- Added deterministic `bk7258-cp-ap-pair-v1` host bundles with exact plain
  v3.1.1.9 RBL, logical `0x250000` container and physical `0x275000` 32+2
  CRC expansion. The verifier binds both components to one generation and
  checks vectors, addresses, sizes, layout, version and digests.
- Passed official header/CRC golden vectors, exact SDK source hashes, two
  deterministic builds, 2 positive/13 negative cases, a fresh real
  `cp_nsh_psram + ap_smp_psram` bundle, and the full dual-image host build.
  No board write occurred and all runtime mutation gates remain false. See
  [N15-A verification](../verification/2026-08-03-n15-a-host-pair-bundle.md).
- Added the CP-only N15-B staging adapter, portable preflight/mutation core,
  deterministic descriptor and shared Flash guard. Complete physical/logical
  pair validation precedes any mutation; each 4 KiB sector and the final slot
  digest are read back under exact B-slot bounds.
- Passed 2 positive/21 negative host cases, `-Werror` core compilation,
  target incremental/full exact-v3.1.1.9 dual builds and final-ELF gate/symbol
  closure. Compile/runtime write gates remain false, no enable setter/command
  exists, and no board write occurred. See
  [N15-B verification](../verification/2026-08-04-n15-b-host-staging.md).
- Added ADR-005 metadata format 1, a portable selector and a team-owned
  read-only Tier-1 adapter. Trusted metadata binds full padded A and exact
  N15-B B-pair validation; erased/corrupt/mixed state fails closed and
  `PENDING_B` never remaps.
- Passed 5 positive/28 negative host cases, four SHA-256 vectors, `-Werror`,
  GCC `-fanalyzer`, exact official v3.1.1.9 source/binary contracts, final
  boot ELF workspace/symbol/four-zero-gate closure, and the complete exact
  dual build. Generation-17 pending metadata remains a host-only artifact;
  factory metadata is erased and no board write occurred. See
  [N15-C verification](../verification/2026-08-04-n15-c-host-boot-selection.md).
- Added the portable one-trial append/read-back controller, SRAM-only metadata
  writer and CP confirm/rollback wrapper. Trial permission exists only for
  the boot that byte-verifies `PENDING_B -> TRIAL_STARTED`; a reset returns A
  unless B later appends `CONFIRMED_B`.
- Passed 4 positive/113 negative cases, 48 per-chunk reset boundaries,
  `-Werror`, GCC `-fanalyzer`, exact SDK source/binary contracts, final Boot/CP
  ELF closure and the complete exact-v3.1.1.9 dual build. All six boot gates
  and CP mutation gates remain closed; no board write occurred. See
  [N15-D verification](../verification/2026-08-04-n15-d-host-trial.md).
- Added the N15-E portable pending-publication controller and CP wrapper.
  Complete live A/B verification precedes metadata mutation; reclaim is
  bounded to erased, failed/rolled-back, or structurally invalid lifecycles,
  and every erase/program/read-back boundary fails closed.
- Passed 5 positive/142 negative cases, 8 erase and 112 program/reset
  boundaries, `-Werror`, GCC `-fanalyzer`, exact source contracts and both
  normal/validation ELF builds without board access. See
  [N15-E verification](../verification/2026-08-04-n15-e-host-publication.md).
- Added the N15-F target-side 5000 ms health-confirm policy with 250 ms
  polling, bound to trusted trial generation, secondary mapping,
  AP-supervisor generation/fault count, healthy/fault-free state and
  monotonic time. The host model uses a 1000 ms fixture only to shorten its
  deterministic tests. Passed 7 positive/15 negative and 5 continuity-reset
  cases.
- Added separate `cp_nsh_ota + ap_smp_psram` validation packaging, exact
  generation token, initially-false runtime gates, fixed 16 MiB upper-PSRAM
  transfer window, standalone descriptor/pending artifacts, transfer verifier
  and dry-run-first WSL2 J-Link loader. The complete validation build and
  normal gates-zero rebuild passed; no J-Link execution or board write
  occurred. See
  [N15-F verification](../verification/2026-08-04-n15-f-host-validation.md).
- Added validation-only generation/operation-bound one-shot target failpoints
  for staging, publication and trial I/O plus one-byte bounded candidate-PSRAM
  corruption. The portable harness passed 7 positive/12 negative cases; BSS
  reset behavior and the absence of fault symbols from normal CP were proven.
- Accepted and implemented ADR-006 metadata format 2 with bank 0 at
  `0x4fb000..0x4fc000`, bank 1 at `0x50a000..0x50b000`, slot-neutral
  inactive-pair staging and shared Boot/CP selection/transition cores.
- Generated and independently verified an ordered 16-package campaign for
  generations 42..57. Every row has a unique RBL/version/timestamp/metadata
  identity, all 16 loader dry-runs passed, generation 56 confirms B and
  terminal generation 57 stages/validates/confirms A from that exact B base.
  Format 2 records a controlled full power cycle for every row; a separate
  verifier rechecks containment, exact fault ordinals, all hashes, the
  cross-generation B base, all three identity classes and reruns the 16
  pair/transfer/loader gates. The validation
  and normal full builds passed and the workspace is restored to the normal
  gates-zero profile. No board/J-Link execution occurred. See
  [N15-V host verification](../verification/2026-08-04-n15-v-host-fault-injection.md).

## Next

1. After fresh, exact-range board-write authorization, define and review one
   minimal physical A-to-B lifecycle and one B-to-A lifecycle.
2. Verify retained N14 services in both confirmed slots and restore the normal
   gates-zero A profile afterward.
3. Treat complete power-removal testing as a separate manual gate; reset-only
   evidence is insufficient.
4. Keep exhaustive campaigns outside the first physical lifecycle gate.
5. Keep publisher signature/key provisioning and anti-rollback outside the
   first N15 security claim.

## Boundaries

- The deployed B is seeded but `boot_selectable=false`; the deployed Tier-1
  still boots A only. The separately built validation artifact is not flashed.
- Normal `writes_enabled=false` remains the deliverable default. Validation
  runtime gates also initialize false and are armed only for one token-bound
  command.
- Use official v3.1.1.9 only. Preserve but do not validate legacy versions.
- Do not modify official NuttX/apps/SDK source or static libraries.
- Never chip erase or include `usr_config`/reserved/tail in migration or OTA
  loader ranges.
- Preserve unrelated untracked QEMU/tests/assets directories.
